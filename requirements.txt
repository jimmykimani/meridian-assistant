# httpx 0.28+ drops `proxies`; OpenAI SDK still passes it until newer releases.
httpx>=0.27,<0.28
openai>=1.40,<2
python-dotenv>=1.0,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
streamlit>=1.40,<2
pytest>=8,<9
pytest-asyncio>=0.24,<1
langsmith>=0.3,<1
